# <誘い> — 詳細要件定義

呼称は日本語も英語もそろえる（→ 02_analysis.md / naming.md）。
追加の要求はまだ無いため、全体が①（当初の要求からある分）にあたる。

**この設計の要点は3つ。**

1. 新設は `Event`（イベント）と `EventEntry`（参加）の2モデルだけ。参照は `event → udb` の一方向
2. イベントの状態は4値（募集中／成立／流れた／取り消された）。**参加者数・開示済み・確定型は保存せず、計算で求める**
3. 匿名の境界は**開示**（開始時刻の30分前）。開示前は誰が押したか誰にも見えない。開示後は見える

## 用語 — 全体の用語集に追加する呼称

| 呼称（これで統一する） | 何を指すか | 言い換えない |
|---|---|---|
| イベント | 1回の誘い。成立するか、流れるか、取り消されて終わる | 企画、飲み会、募集（Slack 投稿の表現としてだけ使う） |
| 参加 | イベントに「参加」を押した記録。取り消されていないものを**有効な参加**と呼ぶ | エントリー、出席、行ける、申し込み |
| 参加者 | 有効な参加を持つ人。立案者を含む | メンバー、顔ぶれ |
| 立案者 | イベントを作った人。参加者の1人に数える。誰かは永久に表示しない | 発案者、主催者、幹事、作成者 |
| 成立人数 | 立案者を含む、成立に必要な人数 | 定員（上限ではなく下限）、最低人数 |
| 開始時刻 | イベントが始まる日時。**開催日を含む**（当日でも未来日でもよい）。開示（−30分）・参加の締切・流れの判定の基準 | 開催日時、集合時間、開催時刻 |
| 成立 | 参加者の数が成立人数に達すること | 開催決定、マッチング |
| 確定型 | 成立人数1のイベント（作った瞬間に成立。「来たい人はどうぞ」の使い方） | 開催確定イベント |
| 開示 | 開始時刻の30分前から、参加者の一覧が参加者にだけ見えるようになること | 公開、発表 |
| 流れる | 開始時刻までに成立しない、または開示前の人数割れで終わること | 不成立、失敗、廃案 |
| （イベントの）取り消し | 開示前に立案者が参加を取り消して、イベントが終わること | 中止、削除、取り下げ |
| 告知先 | ボットが投稿する Slack チャンネル | 通知先、連携チャンネル |

## ER 図

![誘いまわりのER図。左のudbコンテキストに既存の会員（User）があり、会員ID・表示名を持つ。右のeventコンテキストに今回追加するイベント（Event）と参加（EventEntry）がある。イベントはイベントID・立案者ID・内容・場所・成立人数・開始時刻・状態を持つ。参加は参加ID・イベントID・会員ID・取り消し日時を持つ。会員とイベントは1対多（立案者として）、会員と参加は1対多、イベントと参加は1対多。データを持たないものとして、参加者数（取り消し日時が空の参加を数えて求める）、開示済みかどうか（現在時刻が開始時刻の30分前を過ぎたかで求める）、確定型（成立人数が1かどうかで求める）、告知先（環境変数で持ち、モデルにしない）を注記している。参照はevent→udbの一方向で、既存モデルへの属性追加はない](./img/er-invite.svg)

## EntityModel

2つとも今回追加する。既存の `User` には属性を1つも足さない。名前は `naming.md` で確定済み。

### <イベント（Event）>

```scala
case class Event(
  id:                 Option[Id],              // 管理 ID（永続化前は None）
  plannerId:          User.Id,                 // 立案者（User と *:1）。画面には永久に出さない（→ 付録B）
  title:              String,                  // 内容（例："ランチ"、"スマブラ大会"）。自由記述（→ 付録B）
  place:              Option[String] = None,   // 場所。自由記述・任意
  requiredEntryCount: Int,                     // 成立人数。立案者を含む頭数。1 なら作った瞬間に成立（確定型）
  startAt:            LocalDateTime,           // 開始時刻（開催日を含む。未来日も可）。開示・締切・流れの基準
  state:              Status = Status.IS_OPEN, // イベントの状態（区分値、→ 付録B）
  updatedAt:          LocalDateTime = Now,     // データ更新日
  createdAt:          LocalDateTime = Now      // データ作成日
) extends EntityModel[Id]

object Event:

  type   Id = Id.Repr
  object Id extends Entity.Id[Long]

  /** イベントの状態 */
  enum Status(val code: Short) extends EnumStatus[Short]:
    case IS_CANCELED  extends Status(code = -200) // 取り消された（開示前に立案者が参加を取り消した）
    case IS_FAILED    extends Status(code = -100) // 流れた（開始時刻までに成立しなかった／開示前の人数割れ）
    case IS_OPEN      extends Status(code =  100) // 募集中
    case IS_CONFIRMED extends Status(code =  200) // 成立
```

### <参加（EventEntry）>

```scala
case class EventEntry(
  id:         Option[Id],                   // 管理 ID（永続化前は None）
  eventId:    Event.Id,                     // どのイベントへの参加か（Event と *:1）
  userId:     User.Id,                      // 誰の参加か（User と *:1）
  canceledAt: Option[LocalDateTime] = None, // 取り消した日時。None なら有効な参加。一度入れたら戻さない（押し直しは新しい行）
  updatedAt:  LocalDateTime = Now,          // データ更新日
  createdAt:  LocalDateTime = Now           // データ作成日 ＝「参加」を押した日時
) extends EntityModel[Id]

object EventEntry:

  type   Id = Id.Repr
  object Id extends Entity.Id[Long]
```

**ここで型が語っていること**（理由は付録Bの番号を参照）

- `EventEntry` は状態を持たない台帳に近い。有効か取り消し済みかは `canceledAt` の有無で分かる（→ 判断4）
- 参加者数・開示済み・確定型は保存しない。すべて計算で求める（→ 判断2・判断8）
- 立案者の参加も `EventEntry` の1行。立案者かどうかは `Event.plannerId` との比較で分かる（→ 判断3）
- 告知先はモデルにしない。環境変数で持つ（→ 判断7）

**型では守れない決めごと**

- 有効な参加（`canceledAt` が `None`）は、同じ人 × 同じイベントで**最大1行**。
  `canceledAt` は `None` に戻さない。押し直しは新しい行
- イベント作成時は `Event` と立案者の `EventEntry` を**同じトランザクションで**作る。
  成立人数1なら `IS_CONFIRMED` で作る（確定型）
- `state` の更新は「押す」「取り消す」「時刻で締める」の3つの操作関数に絞る。
  **成立しているかの正は `state`。** 参加者数から数え直さない（→ 判断1）
- 「参加」を押せるのは `IS_OPEN` / `IS_CONFIRMED` で、開始時刻より前のイベントだけ
- `requiredEntryCount`（1以上）と `plannerId` は作成後に変更しない
- `startAt` は開催日を含む日時。当日でも未来日でもよいが、**立案者が1つに決める**（候補日は持たない → 判断11）

**保存されるデータの例**（会員 A〜D の部署）

`Event`

| id | plannerId | title | requiredEntryCount | startAt | state | 何が起きたか |
|---|---|---|---|---|---|---|
| 21 | A | 今日ランチ | 3 | 12:00 | IS_CONFIRMED | A が作成 → B・C が押して3人で成立 |
| 22 | B | スマブラ大会 | 1 | 19:00 | IS_CONFIRMED | 確定型。**作った瞬間から成立** |
| 23 | C | 飲み | 4 | 19:30 | IS_FAILED | 2人までしか集まらず、開始時刻が来て流れた |
| 24 | D | 朝ランニング | 2 | 8/23 07:00 | IS_OPEN | **5日後の未来日イベント**。日付を指定して募集中 |

`EventEntry`（イベント21の分）

| id | eventId | userId | canceledAt | createdAt | 何が起きたか |
|---|---|---|---|---|---|
| 101 | 21 | A | `None` | 10:02 | 立案者の参加。イベント作成と同時にできる |
| 102 | 21 | B | **10:31** | 10:15 | B は一度押して取り消した |
| 103 | 21 | C | `None` | 10:40 | C が押して2人 |
| 104 | 21 | B | `None` | 11:05 | **B の押し直し（行102は書き換えず、新しい行）**。3人で成立 |

B が迷った履歴（行102・104）はそのまま残るが、画面に出るのは有効な参加だけ。

## 区分値の考え方

マイナスが終了、プラスが進行中〜正常。遷移は4本だけ。

| 遷移 | きっかけ |
|---|---|
| `IS_OPEN` → `IS_CONFIRMED` | 参加者数が成立人数に達した（成立の投稿と同時） |
| `IS_CONFIRMED` → `IS_FAILED` | **開示前**の取り消しで成立人数を割った（流れたの投稿と同時） |
| `IS_OPEN` → `IS_FAILED` | 開始時刻が来ても成立しなかった |
| `IS_OPEN` / `IS_CONFIRMED` → `IS_CANCELED` | **開示前**に立案者が参加を取り消した |

- 「成立」を参加者数からの導出にしないのは、**開示後は人数が割れても成立のままにする**
  （数と状態がわざと食い違う）仕様のため（→ 判断1）
- **開示済みは状態に入れない。** 「現在時刻 ≥ 開始時刻 − 30分」で求まる（→ 判断2）
- **開催済みも状態に入れない。** `IS_CONFIRMED` のまま開始時刻を過ぎたら開催とみなす（→ 判断10）

## 判定ルール

### いつ何をするか

| きっかけ | すること |
|---|---|
| イベントを作った | `Event` と立案者の `EventEntry` を1行ずつ作る（成立人数1なら `IS_CONFIRMED` で）。ボットが募集を投稿（名前は出さない。確定型なら「やります。来たい人どうぞ」の体裁） |
| 「参加」を押した | 有効な参加が無いことを確認して1行足す。成立人数に達したら `IS_CONFIRMED` にして成立を投稿 |
| 参加を取り消した | `canceledAt` を書く。**開示前**に成立人数を割ったら `IS_FAILED` にして流れたを投稿。**開示後**は `state` を変えない（一覧から消えるだけ） |
| 立案者が参加を取り消した | **開示前**：`IS_CANCELED` にして取り消されたを投稿（人数は関係ない。確定型を止める唯一の手段）。**開示後**：普通の取り消しと同じ（→ 判断6） |
| 開始時刻が来た | `IS_OPEN` のままなら `IS_FAILED` にして流れたを投稿。`IS_CONFIRMED` はそのまま（開催とみなす）。**この判定は分単位のスケジューラが実行する**（画面を開いたときの判定だと、誰も開かなければ投稿されないため） |
| 開始時刻の30分前が来た | **何も書き込まない**。詳細ページの表示が変わるだけ（スケジューラも不要） |

ボットの投稿は、状態の変更と**切り離す**。正はあくまで状態で、投稿に失敗しても
イベントの状態は巻き戻さない。再試行や投稿の編集・削除もやらない（→ 付録A）。

### 誰に何が見えるか

| 情報 | 開示前 | 開示後 |
|---|---|---|
| 内容・開始時刻・場所 | URL を開けば誰でも | 同左 |
| 参加者の一覧・人数 | **誰にも見えない** | **参加者にだけ**見える（有効な参加のみ） |
| 立案者が誰か | 誰にも見えない | 誰にも見えない（**永久**） |
| 自分が押しているか | 本人にだけ見える | 本人にだけ見える |

Slack に出す情報（募集・成立・流れた・取り消されたの投稿、URL のプレビュー）は
「URL を開けば誰でも」の行の範囲に限る。**人数・立案者・参加者は絶対に載せない。**

参加者数は、その `eventId` の `EventEntry` のうち `canceledAt` が `None` の行数（立案者の行も含む）。
この数を使ってよいのは遷移の判定と開示後の一覧表示だけで、「成立しているか」は `state` を見る。

## 置き場所（コンテキスト）

新しいコンテキスト `event` に置く（→ naming.md）。`Event` / `EventEntry` は `User.Id` を参照する側
なので `udb` には置けない。参照は `event → udb` の一方向。テーブルは `event` / `event_entry`
（エンティティ名がコンテキスト名で始まるときは重ねない規則）。

---

# 付録A：今回やらないこと

- **日程調整**（候補日を並べてみんなで決める形は作らない。開催日時は立案者が1つに決める → 判断11）
- **開催前のリマインド投稿**（ボットの投稿は募集時の1回だけ。忘れられて流れたら記録に残るだけ）
- **立案者の記名オプション**（匿名か記名かを選べる形は作らない。まず匿名一本で始める）
- **宛先の手選び・複数チャンネル**（告知先は環境変数の1チャンネルのみ）
- **参加の承認制**（URL ＋ ログインで誰でも参加できる。漏えいリスクは社内ツールとして許容）
- **個人向け通知（DM）**（通知はボットのチャンネル投稿だけ。開示の確認はアプリを開いて行う）
- **ボット投稿のランダム遅延**（投稿タイミングから立案者を推測される問題は対策しない）
- **開催の記録・実施報告・精算**（成立のまま開始時刻を過ぎたら開催とみなす）
- **イベントの編集**（内容・時刻・人数は作成後に変更できない。取り消して作り直す → 判断9）
- **告知先の設定画面**（環境変数で代替。01 の業務フロー1はデプロイ設定として実現する）
- **「行けたら行く」などの曖昧な回答**（参加か、押さないかの2択）
- **過去イベントの一覧・集計画面**（データは残すが、振り返る画面は作らない）
- **イベントへのコメント・チャット**（成立後の相談は開示された参加者同士が Slack で直接やる）
- **Slack 投稿の再試行・編集・削除**（投稿は投げっぱなし。失敗しても状態が正で、巻き戻さない。
  投稿のメッセージ ID を写す列も持たない。再試行が要る規模になったら列ごと見直す）
- **管理者によるイベントの強制取り消し・通報**（不適切な募集への対処は作らない。
  6人の部署では口頭で足りる。他部署へ広げるときに管理者アクターごと検討する）

# 付録B：判断の記録

全件が①（当初の要求の範囲で決めたこと）。番号は通し番号。

### **1. イベントの状態（`state`）を保存するか、導出するか**

**保存する（4値）。** 「流れた」「取り消された」は出来事で、参加の行からは求まらない。
「成立」も、開示後は人数が割れても成立のままにする仕様のため、**数と状態はわざと食い違う**。
却下案は全部導出——この仕様を表現できない。
**代償**：参加者数との二重管理。更新経路を3つの操作関数に絞って対応する。

### **2. 開示済みを状態に含めるか**

**含めない。** 時刻だけで求まり、書き込むきっかけ（誰かの操作）が無い。
却下案は `IS_REVEALED` の5値目——時刻監視のバッチが要り、遅れれば「30分前なのに開示されない」が起きる。
**代償**：「成立していて開示済みか」の判定が `state` と時刻の2軸になる。

### **3. 立案者の参加を `EventEntry` の行として作るか**

**作る。** 「参加者＝有効な参加の集合」という1つの定義で全部が説明できる。
却下案は行を作らず「＋1」で数える——数える箇所すべてに分岐が散らばる。
立案者の印は持たない（`plannerId` との二重管理になる）。
**代償**：立案者の行の取り消しだけ特別扱いが要る（→ 判断6）。分岐は取り消しの関数1箇所に閉じる。

### **4. 参加の取り消しを、行の削除で表すか、取り消し日時の追記で表すか**

**`canceledAt` の追記。行は消さず、押し直しは新しい行。**
却下案は物理削除（取り消した事実ごと消える）と、`canceledAt` を `None` に戻す案
（可変な列を履歴の根拠にすることになる）。
残した履歴の読み手は**分析ではなく調査**（問い合わせ対応）。画面には一切出さない。
**代償**：有効な参加を探す条件に常に `canceledAt IS NULL` が付く。忘れると取り消した人を数えてしまう。

### **5. 成立後の人数割れ（開示前）を、終了にするか、募集中に戻すか**

**終了（`IS_FAILED`）。**
却下案は募集中に戻す——成立⇄流れの投稿が繰り返されてチャンネルがノイズになり、
「成立しました」の重みが消える。
**代償**：復活の余地が無い。割れた瞬間に終わる。まだやりたければ作り直す。

### **6. 立案者の取り消しの扱い（01 の判断メモから引き継ぎ）**

**開示前はイベントごと取り消し（`IS_CANCELED`）、開示後は普通の取り消しと同じ。**
専用の取り下げボタンは作らない（匿名の立案者にだけ出すボタンは、それ自体が正体の手がかりになる）。
開示後に限って通常扱いにするのは、一覧から消えた人と同時にイベントが消えると立案者だと推測できるため。
**代償**：開示後は誰もイベントを止められない。相談は開示された参加者同士に委ねる。

### **7. 告知先をモデルにするか、環境変数にするか**

**環境変数。** 全体で1件しかない設定に、モデル・テーブル・リポジトリの4点セットは重い。
**代償**：誰がいつ設定したか残らず、変更にはデプロイが要る。
複数チャンネルの要求が出たらエンティティに昇格する（→ 判断が変わる条件）。

### **8. 確定型を区分値で持つか、成立人数の解釈で表すか**

**解釈で表す（成立人数1＝確定型）。** 種別を持つと、種別と人数の組み合わせに矛盾がありうる。
**代償**：`requiredEntryCount == 1` という条件が散らばりやすい。判定はモデルのメソッドに1箇所だけ書く。

### **9. イベントの編集を許すか**

**許さない。取り消して作り直す。** 編集を許すと、成立人数を後から下げて即成立させる等、
すでに押した人の前提を裏切る変更ができる。参加の根拠が動くなら押し直しであるべき。
**代償**：タイポ1つでも作り直しになる。

### **10. 開催済みの状態を持つか**

**持たない。** `IS_CONFIRMED` のまま開始時刻を過ぎたら開催とみなす。
状態にするには実施報告（やらないと決めた）か時刻バッチ（判断2と矛盾）が要る。
**代償**：開催済みの集計は「`IS_CONFIRMED` かつ `startAt` が過去」の2条件になる。

### **11. 未来日のイベントを許すか**

**許す。** `startAt` は開催日を含む日時で、当日でも未来日でもよい
（「この日にランニングに行こう」という発注者の要求）。
却下案は候補日を並べる日程調整——成立条件が「人数」の一軸でなくなり、このアプリの軽さが死ぬ。
日付は立案者が1つに決める。
**代償**：募集から開催まで日が空くと忘れられる（リマインドは無い → 付録A）。
成立後にキャンセルできる期間も長くなり、人数割れで流れる機会が増える。

## 判断が変わる条件（まとめ）

| 条件 | どうなるか | 関連する判断 |
|---|---|---|
| 複数チャンネル・グループ分けの要求が入る | 告知先を `Channel` エンティティに昇格する | 判断7 |
| 開催実績を集計したい要求が入る | 開催済みの状態、または実施報告を検討する | 判断10 |
| 「人数割れしても復活させたい」声が出る | 募集中へ戻す案を再検討（投稿のノイズ対策とセットで） | 判断5 |
| カテゴリ別の集計（ランチ率など）がしたくなる | `title` 自由記述をやめ、区分値を検討する | EntityModel |
| 宛先の手選びの要求が入る | 「見えるのに押せない人」問題が復活する。宛先の概念を設計し直す | 判断7 |
| 参加の締切を開始時刻より早くしたい | `startAt` とは別に締切時刻を持ち、開示との前後関係を再設計する | EntityModel |
| 未来日のイベントが忘れられて流れがちになる | 前日・30分前などのリマインド投稿を検討する | 判断11 |
| 「誰の企画か知りたい」という声が出る | 立案者の記名オプション（作成時に匿名か記名かを選ぶ）を検討する。記名なら取り下げ操作の前提（判断6）も変わる | 判断6 |
