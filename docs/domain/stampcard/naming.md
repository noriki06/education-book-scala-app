# 命名の選定記録

対象はマスタ／保有カード／スタンプの3階層。

## 評価軸

APPENDIX_02 の7軸に、⑧を加えた8軸。①③④⑧を重く見る。4点以下は致命的欠点とする。

① 役割差 ② 正確さ ③ 並び ④ 業務語 ⑤ 長さ ⑥ 拡張余地 ⑦ 多重度
⑧ 親子の向き（構造上の親子と、接頭辞が示す親子が一致するか）

⑧ を足したのはレビューの指摘から。3階層になると、深い階層ほど名前が短くなる並びが起こりやすく、
`Cart` < `CartItem` < `CartItemOption`（深いほど長い）と逆向きになると階層を取り違える。

**④ の扱いに注意。** 要求文にある業務語は「スタンプ」だけで、
カードやその種別を業務が何と呼ぶかは確認できていない（→ 01_requirements.md の質問リスト）。
そのため ④ は「要求文の語（スタンプ）を残せているか」として採点し、
カード・種別の呼び方については減点しない。確認できていない語を業務語として扱わない。

## 候補と評価

| マスタ / 保有カード / スタンプ | ① | ② | ③ | ④ | ⑤ | ⑥ | ⑦ | ⑧ | 総合 | 却下理由 |
|---|---|---|---|---|---|---|---|---|---|---|
| **Reward / RewardCard / RewardCardStamp** | 9 | 9 | 10 | 9 | 8 | 9 | 9 | 10 | **9** | 採用 |
| Coupon / CouponCard / CouponCardStamp | 9 | 7 | 10 | 9 | 8 | 8 | 9 | 10 | 8 | Coupon は「発行され、使ったら消える券」の語。貯めている途中（必要数に届いていない）カードを `CouponCard` と呼ぶのは早すぎる |
| Campaign / CampaignCard / CampaignCardStamp | 8 | 8 | 9 | 9 | 7 | 8 | 9 | 10 | 7 | Campaign は期間限定の催しを指す語。恒常的な通常の種別まで含めると意味が広がりすぎる |
| Reward / RewardCard / RewardStamp | 7 | 9 | 10 | 9 | 9 | 9 | 9 | 7 | 7 | `Card` を省いた分は短いが、`RewardCard` の子であることが名前から読めない（`Reward` 直下に見える） |
| StampCardType / StampCard / Stamp | 9 | 9 | 10 | 9 | 9 | 8 | 9 | **4** | 6 | `Stamp` が最上位と中間の両方に現れ、深い階層ほど名前が短い。`Cart` 系列と逆向き |
| StampCard / StampCardIssue / StampCardIssueStamp | 9 | 8 | 10 | 6 | 5 | 7 | 9 | 10 | 6 | `Stamp` が1つの名前に2回出る。`Issue`（発行）も技術寄りで、孫の名前が28字になる |
| StampCard / StampCardIssue / Stamp | 8 | 8 | 10 | 9 | 8 | 6 | 9 | 7 | 6 | `StampCard` を種別の名前に使うと、会員が持つ1枚と紛らわしい。⑥ も低い |

## 決定打

**採用は `Reward` / `RewardCard` / `RewardCardStamp`。対抗は `Coupon` 系（総合8）。**

⑧ で満点（10）を取れる案は複数あるが、`Reward` 系が勝つ決定打は ②⑥。

**② 正確さ：`Reward` は「特典」そのものの語で、誤解の余地がない。**
`Coupon` は「発行され、使われたら消える券」を指す語。貯めている途中（まだ特典に届いていない）カードを
`CouponCard` と呼ぶのは早すぎる——0個の状態でも `Coupon` と呼ぶことになってしまう。
`Reward` には「発行されて消える」という含意がなく、「特典を得るためのカード」と読めるので、
貯め始めの0個でも、必要数を満たした状態でも、同じ名前で通る。

**⑥ 拡張余地：`Reward` は誕生日特典のような、スタンプを使わない特典も同じ枠に収められる。**
マスタが `Reward`（特典の定義）である以上、必要スタンプ数・配布期間・特典の内容を持つ形は変えずに、
将来「スタンプを貯めずに条件を満たしたら発行される特典」が来ても、同じ `Reward` の1種として扱える。
`StampCard` 系の名前で始めていたら、マスタの名前自体がスタンプを前提にしてしまい、この余地がなかった。
03_design.md 付録B で「スタンプ以外の発行理由が来たら `Coupon` に切り替える」としていた代案は、
この採用によって不要になった——`Reward` そのものが、その拡張余地を先取りしている。

`Coupon` を採らなかったのは②が理由だが、**`Coupon` という語は空けておく。**
将来、発行されたら消える実際のクーポン券（配布して終わりのもの）が要件に入ったときのために取っておく。
`CouponCard` で確定させてしまうと、そのとき名前が使えなくなる。

**日本語の呼称も英語に合わせる。** リワード／リワードカード／スタンプ。

要求文にある語は「スタンプ」だけで、カードやその種別を業務が何と呼ぶかは確認できていない。
確認できていない語（「スタンプカード」）を業務語として扱い、それに英語名を合わせようとすると、
実際の呼び方が判明したときに二重に直すことになる。

日本語と英語を1対1で対応させれば、訳の対応表が要らない。
用語表の「言い換えない」欄に「スタンプカード」を入れて、途中で戻ることを防ぐ（→ 03_design.md）。

## 名前を並べて確認する

**クラス名**
- MenuItem                 ← 既存（common）
- Reward                   ← 今回追加（common）
- Cart ... Payment         ← 既存（sales）
- RewardCard               ← 今回追加（sales）
- RewardCardStamp          ← 今回追加（sales）

**テーブル名**
- common_menu_item              ← 既存
- common_reward                 ← 今回追加（13字）
- sales_cart ... sales_payment  ← 既存
- sales_reward_card             ← 今回追加（17字）
- sales_reward_card_stamp       ← 今回追加（23字）

深い階層ほど名前が長くなる：`Reward`(6) < `RewardCard`(10) < `RewardCardStamp`(15)。
`Cart`(4) < `CartItem`(8) < `CartItemOption`(14) と同じ向き。

## 判断が変わる条件

| 条件 | どうなるか |
|---|---|
| 業務が「スタンプカード」と呼んでいると確認できる | ④ の前提が変わる。日本語の呼称をスタンプカードに戻し、英語名との対応表を持つかを検討する |
| 発行されたら消える実際のクーポン券が要件に入る | `Coupon` という語が使える。`Reward` の1種として持たせるか、別に並べるかを検討する |
| 期間限定の種別しか扱わなくなる | `Campaign` 系が有力になる。恒常的な種別が同じ枠にある限りは意味が広がりすぎる |
