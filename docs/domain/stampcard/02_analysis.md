# <スタンプカード> — 思考過程（メモ・提出しない）

## 名詞と動詞
名詞：会員、店舗、注文、スタンプ、有効期限、スタンプカード、無料特典、ハンバーガー、アプリ

動詞：ログインする、支払う、(無料バーガーを)選ぶ、スタンプを押す、貯める、スキャンする、キャンセルする、
　　　スタンプを消す、使用済みにする、記録する、(いま何個あるか)見る、(期限切れか)判定する

| 種類 | 動詞 | 行き先 |
|---|---|---|
|状態を変える|スタンプを押す/スタンプを消す/使用済みにする|スタンプを1件記録する・削除する・使用日時を記録することで表す|
|値を返す|いま何個あるか数える　/期限切れか判定する|モデルのメソッド|

表に入らないもの(既存機能の動詞)
ログインする、支払う、(無料バーガーを)選ぶ。
スタンプを押すきっかけにはなるが、今回のモデルの操作ではない。


## ふるい分け

| 名詞 | 仕分け | 理由 |
|---|---|---|
| 会員(既存) | **エンティティ** |スタンプの持ち主。今回は変更しない  |
| 店舗(既存) | **エンティティ** |どの店舗で押したかを持つ。店舗をまたいで共通なので、店舗ごとに集計しない|
| 注文(既存) | **エンティティ** |スタンプを押すきっかけ。スタンプ側がOrder.Idを参照する。Order自体は変更しない  |
|スタンプ|**エンティティ**|日付を確認できる必要がある、いつどの注文かを確認可能|
|有効期限|**値オブジェクト**|スタンプを押した日から一年後|
|スタンプカード|対象外（＝計算結果）|  |
|無料特典|対象外|---|エンティティ？
|ハンバーガー（既存）|エンティティ|---|
|アプリ|対象外|---|


## ユビキタス言語（候補）
| 日本語 | 英語名 | 意味 | 備考 |
|---|---|---|---|
|スタンプ|SalesStamp|注文1回につき1個獲得し、特典交換に使う単位|多重度(Order と 1:1、会員と 1:*)|
|有効期限|deadline|最後のスタンプから１年後の日付|値オブジェクト|
|スタンプカード|モデルなし|会員がいま持っている未使用スタンプのまとまり|保存しない。未使用スタンプの件数として算出


## 名前を並べて確認する
（既存の名前に混ぜて名前順に。クラス名とテーブル名の両方）

## コンテキストの判断
| コンテキスト | 記号 | 変更の理由 | ライフサイクル | 管理する人 |
|---|---|---|---|---|
| 注文 | `sales` | スタンプを押す、スタンプを消去する| １年間（履歴としては永久） | 会員の注文でスタンプが押され



### 判断の記録（迷ったところと、その決着。02_BURGER_DOMAIN の「設計上の論点」と同じ形式で）

#### 「いま何個たまっているか」を数値として保存するか、押した記録（履歴）から数え上げるか。
スタンプ押した際の購入履歴が付随しないから押した記録（履歴）から数え上げて、
１０個以上は新しいスタンプカードにするようにして、
１０個で一枚のスタンプカードにして、10個以上は新しいカードにつけるようにして表示する。
そのスタンプカードで期限内であればバーガーを買えて、購入履歴で有効期限も管理できる
#### 有効期限「最後の注文から1年」は、どのタイミングで、どう判定するか。期限切れを表す状態を持つのか、日付から計算するのか。
- アプリのスタンプカードを開いたタイミングで、日付で１年経過を確認してスタンプカードのスタンプ数を確認し、
- 購入者も今なら1個無料かどうかを確認するため


#### 注文がキャンセルされたら、スタンプはどうなるか（要求に書かれていません。あなたの判断と、その理由を書いてください）。
- 購入したら（アプリでのオンライン決済、店舗での決済）スタンプ一つ、途中キャンセルになったら、取り消し
- 店舗での購入際に、受け取り完了でスタンプだとオペレーションが増えるので、
#### 「10個たまったら無料」の 無料になった1回 は、何として記録されるか。
次に見たとき「使用済みの10個」と「たまり始めた新しい1個」を区別できるか。
