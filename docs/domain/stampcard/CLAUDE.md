# docs/domain/stampcard — スタンプドメインの補足

書き方の共通ルールは `docs/domain/CLAUDE.md`。
**それが読み込まれていなければ、先に `docs/domain/CLAUDE.md` を読むこと。**
ここにはこのドメイン固有の経緯と落とし穴だけを書く。

## いまの状態

設計4点セットは書き上がっている。**実装（Scala）はまだ1行も無い。**
`Reward` / `RewardCard` / `RewardCardStamp` の3モデルを `edu/common` と `edu/sales` に作るところから。

- `Reward` → `common`（本部が管理する定義。`common_reward`）
- `RewardCard` / `RewardCardStamp` → `sales`（`sales_reward_card` / `sales_reward_card_stamp`）
- 参照は `sales` → `common` の一方向。`udb` には置けない（`Order.Id` を参照するため逆流する）

## 呼称（過去に事故った箇所）

`Reward` を指す語は **「リワード」だけ**。過去に「マスタ」「種別」と書き分けてしまい全面修正した。
`RewardCard` は **「リワードカード」だけ**（`naming.md` で「保有カード」と書いていた時期がある）。

用語表の「言い換えない」欄に **マスタ / 種別 / 保有カード** を登録済み。使わない。
ただし**その欄そのものは禁止語を列挙する場所**なので、置換してはいけない。

## 実装で真っ先に踏む落とし穴（設計書の「型では守れない決めごと」より）

この3つは型でもDB制約でも守れない。実装するとき明示的にテストを書く。

1. **中断中のカード（`suspendedAt` が `Some`）を失効させてはいけない。**
   日次バッチの対象から除く。除くと中断中に期限が来て `IS_EXPIRED` になり、
   再開時に中断日数を加算する機会が永久に失われる
2. **発行済み枚数を数えるとき `state` で絞り込んではいけない。**
   `userId` と `rewardId` が一致する `RewardCard` の全件を数える（期限切れ・使用済みも含む）。
   絞ると失効や交換のあとに期間限定の2枚目が作れてしまう
3. **`IS_FILLED` のカードを `expiredAt` だけで判定してはいけない。**
   特典が使えるのは `expiredAt` ＋ `Reward.bonusExtensionPeriod` まで。
   `expiredAt` 単独で切ると、獲得済みの特典を延長期間の分だけ早く失効させる

あわせて、**収集中のカードを探すクエリには必ず `suspendedAt` が `None` の条件を付ける**。
これを忘れると中断中のカードにスタンプが押される。

## 期限が2つあること

1枚のカードに期限が2つある。どちらを見るかは `state` で決まる。

| state | 見るべき期限 |
|---|---|
| `IS_COLLECTING` | `expiredAt`（スタンプを押せる期限） |
| `IS_FILLED` | `expiredAt` ＋ 延長期間（特典を使える期限） |

特典の有効期限は**保存しない**（`expiredAt` から計算する）。理由は付録Bにある。

## 追加の要求が入っている

このドメインは追加の要求で大きく変わっているため、3ファイルすべてが①②③の構成になっている。
**③（追加の要求で加わった分）が大半。** リワード・カードの状態・特典の延長期間・発行上限・
中断と再開は、いずれも追加の要求で初めて入った概念。

`_sample/` は初期の下書き。**参照しない。**
