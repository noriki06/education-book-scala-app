# docs/domain/stampcard — スタンプドメインの補足

書き方の共通ルールは `docs/domain/CLAUDE.md`。
**それが読み込まれていなければ、先に `docs/domain/CLAUDE.md` を読むこと。**
ここにはこのドメイン固有の落とし穴と経緯だけを書く。

**判断の理由は `03_design.md` の付録Bが正。このファイルに複製しない。**
理由を知りたいときは付録Bを読む。ここに書き写すと、設計を直したときに片方だけ古くなる。

## いまの状態

設計4点セットは書き上がっている。**実装（Scala）はまだ1行も無い。**
`Reward` / `UserRewardCard` / `UserRewardCardStamp` の3モデルを作るところから。

| 日本語 | クラス | テーブル | コンテキスト |
|---|---|---|---|
| リワード | `Reward` | `common_reward` | `common` |
| ユーザーリワードカード | `UserRewardCard` | `sales_user_reward_card` | `sales` |
| スタンプ | `UserRewardCardStamp` | `sales_user_reward_card_stamp` | `sales` |

参照は `sales` → `common` の一方向。`udb` には置けない（`Order.Id` を参照するため逆流する）。
**既存モデル（`User` / `Shop` / `Order` / `MenuItem`）には属性を1つも足さない。**

## 呼称（過去に事故った箇所）

`Reward` を指す語は **「リワード」だけ**。過去に「マスタ」「種別」と書き分けてしまい全面修正した。
`UserRewardCard` は **「ユーザーリワードカード」だけ**（`naming.md` で「保有カード」と書いていた時期がある）。

用語表の「言い換えない」欄に **マスタ / 種別 / 保有カード / スタンプカード** を登録済み。使わない。
ただし**その欄そのものは禁止語を列挙する場所**なので、置換してはいけない。

## 期限が2つあること

1枚のカードに期限が2つある。どちらを見るかは `state` で決まる。

| state | 見るべき期限 |
|---|---|
| `IS_COLLECTING` | `expiredAt`（スタンプを押せる期限） |
| `IS_FILLED` | `expiredAt` ＋ `Reward.bonusExtensionPeriod`（特典を使える期限） |

特典の有効期限は**保存しない**（`expiredAt` から計算する）。

## 実装で真っ先に踏む落とし穴

型でもDB制約でも守れない。**明示的にテストを書く。**

1. **中断中のカード（`suspendedAt` が `Some`）を失効させてはいけない。**
   日次バッチの対象から除く。除くと中断中に期限が来て `IS_EXPIRED` になり、
   再開時に中断日数を加算する機会が永久に失われる
2. **発行済み枚数を数えるとき `state` で絞り込んではいけない。**
   `userId` と `rewardId` が一致する `UserRewardCard` の全件を数える（期限切れ・使用済みも含む）。
   絞ると失効や交換のあとに発行上限を超えるカードが作れてしまう
3. **`IS_FILLED` のカードを `expiredAt` だけで判定してはいけない。**
   特典が使えるのは `expiredAt` ＋ `Reward.bonusExtensionPeriod` まで。
   `expiredAt` 単独で切ると、獲得済みの特典を延長期間の分だけ早く失効させる
4. **収集中のカードを探すクエリには必ず `suspendedAt` が `None` の条件を付ける。**
   忘れると中断中のカードにスタンプが押される
5. **`UserRewardCardStamp` のレコードを後から作り直さない。**
   `createdAt` が付与日時そのものなので、作り直すと有効期限の起点がズレる
6. **`state` と実体は必ず一致させる。**
   更新経路を「スタンプを押す」「使用済みにする」「差し戻す」「失効させる」の操作関数に絞り、
   個別に書き換えない

## 申し送り（反映済み）

別チャットで作った引き継ぎメモにあった、設計書へ未反映だった判断4件は**すべて `03_design.md` に反映済み**。

- 付与日時を `createdAt` で足りるとした代償 → 付録B「付与日時を専用のカラムで持つか」に追記、
  付録Aにも「データ移行・手動でのスタンプ遡及付与」としてスコープ外を明記
- 「判断が変わる条件」の一覧表 → 付録Bの末尾「判断が変わる条件（まとめ）」に集約
- `Coupon` 新設時に `usedOrderId` / `usedAt` を移す件 → 上記一覧表の1行として記録（理由：二重管理の回避）
- 複数の期間限定リワードの同時開催 → 当初は許さないと判断していたが、誕生日クーポンのような
  個人単位の期間限定リワードを見据えて**許す判断に変更**。中断中のカードは複数枚になりうる前提とし、
  再開順は後入れ先出し（LIFO）に決めた（→ 付録B「期間限定のリワードの同時開催を許すか、許さないか」）

`_sample/` は初期の下書き。**参照しない。**
