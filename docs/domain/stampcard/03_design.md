# <スタンプカード> — 詳細要件定義

## 用語 — 全体の用語集に追加する呼称
（**呼称をそろえる取り決め。個別の仕様書で用語を定義しない**。追加を申請する分だけ挙げる）
| 呼称（これで統一する） | 何を指すか | 言い換えない |
|---|---|---|
|スタンプ|スタンプカードに注文ごとに押され、特典交換に必要な数を数える単位。|ポイント、来店記録|
|有効期限|その会員が最後にスタンプを得てから1年後の日付。この日を過ぎると有効スタンプとして数えない|「失効予定日」「期限」「有効期間」|
|有効スタンプ|	未使用で、無効化されておらず、期限内のスタンプ|「残スタンプ」「使えるスタンプ」|
|スタンプカード|会員が保有するスタンプのまとまり|「台紙」「カード」|
|使用済み|	特典交換で消費されたスタンプの状態|「消化」「消費済み」|
|無効|キャンセル・未受け取りにより、数に含めなくなったスタンプの状態|「削除」「取り消し」|
|無料特典|	有効スタンプ10個と引き換えに、ハンバーガー1個を無料で受け取れること|「クーポン」「割引」「サービス券」|


## ER 図
（**データの繋がりだけ**。日本語の項目名と多重度が読めればよい）
![スタンプまわりのER図。左に既存の会員・店舗・注文が並び、右に今回追加するスタンプがある。会員とスタンプは1対多、店舗とスタンプは1対多、注文とスタンプは1対1](./img/er-stamp.svg)

## EntityModel
### <スタンプ>

case class SalesStamp(
  id:        Option[Id],                     // 管理 ID（永続化前は None）
  shopId:    Shop.Id,                        // どの店舗での購入か
  orderId:   Order.Id,                       //どの注文のスタンプか
  deadline: LocalDate,                       // 有効期限
  usedAt: Option[LocalDateTime],   　　　　　// 特典交換で使用した日時（未使用なら None）
  updatedAt: LocalDateTime  = Now,           // データ更新日
  createdAt: LocalDateTime  = Now            // データ作成日
) extends EntityModel[Id]

object ShopBusinessHourOverride:

  type   Id = Id.Repr
  object Id extends Entity.Id[Long]

**ここで型が語っていること**
- （必須 / 任意 を型がどう表しているか）
usedAtのOption の None は「未使用」
有効期限を時刻で判断せずに、日にちで判断する

**型では守れない決めごと**
- （一意制約・属性どうしの連動など。**モデルは形だけを書き、ロジックは載せない**）
スタンプが１０個たまると一個バーガー無料、１１個目のスタンプはまた１個目換算になる
有効期限はスタンプごとに１年間

**保存されるデータの例**
（パターンごとに 1 行ずつ、**実際に入る値**で）

## 区分値の考え方
（業務の言葉と区分値の数が合わないなら、**なぜ合わなくてよいか**を書く）

## <複数のエンティティにまたがる判定ルール>
（あれば書く。無ければ節ごと省く）



## 置き場所（コンテキスト）
**判断：既存の sales コンテキストに入れる。新しく作らない。**
**理由：スタンプは会員の注文で押される、定員による消去も実施可能、注文の詳細も持つため**

---

# 付録A：今回やらないこと
 - 有効期限間近になった時のお知らせ
 - スタンプの譲渡の有無
 - 無料バーガーの選択の範囲設定（どれでも買えるか）


# 付録B：判断の記録
### 論点1：<迷ったこと>
**判断：**（理由 3〜6 行）



### 判断の記録（迷ったところと、その決着。02_BURGER_DOMAIN の「設計上の論点」と同じ形式で）

#### 「いま何個たまっているか」を数値として保存するか、押した記録（履歴）から数え上げるか。
スタンプ押した際の購入履歴が付随しないから押した記録（履歴）から数え上げて、
１０個以上は新しいスタンプカードにするようにして、
１０個で一枚のスタンプカードにして、10個以上は新しいカードにつけるようにして表示する。
そのスタンプカードで期限内であればバーガーを買えて、購入履歴で有効期限も管理できる
#### 有効期限「最後の注文から1年」は、どのタイミングで、どう判定するか。期限切れを表す状態を持つのか、日付から計算するのか。
- アプリのスタンプカードを開いたタイミングで、日付で１年経過を確認してスタンプカードのスタンプ数を確認し、
- 購入者も今なら1個無料かどうかを確認するため


#### 注文がキャンセルされたら、スタンプはどうなるか（要求に書かれていません。あなたの判断と、その理由を書いてください）。
- 購入したら（アプリでのオンライン決済、店舗での決済）スタンプ一つ、途中キャンセルになったら、取り消し
- 店舗での購入際に、受け取り完了でスタンプだとオペレーションが増えるので、
#### 「10個たまったら無料」の 無料になった1回 は、何として記録されるか。
次に見たとき「使用済みの10個」と「たまり始めた新しい1個」を区別できるか。
