# <お誘い> — 詳細要件定義

呼称は日本語も英語もそろえる（→ 02_analysis.md / naming.md）。
追加の要求はまだ無いため、全体が①（当初の要求からある分）にあたる。
お誘い（`event`）とお店（`shop`）の2つの文脈を、1つのアプリ（Osasoi）の設計として扱う。

**この設計の要点は5つ。**

1. ドメインのモデルは5つ——会員（`User`・udb）、イベント（`Event`）・参加（`EventEntry`・event）、店（`Shop`）・評価（`ShopReview`・shop）。認証用のモデルは実装時に udb へ足す（→ 置き場所）
2. 参照は `event → udb`・`shop → udb`・**`event → shop`（場所として選んだ店の ID）**の一方向。shop は event を一切知らない
3. イベントの状態は4値（募集中／成立／流れた／取り消された）。**参加者数・開示済み・確定型は保存せず、計算で求める**
4. 匿名の境界は**開示**（既定は開始時刻の30分前。イベントごとに変えられる）。開示前は誰が押したか誰にも見えない。開示後は見える
5. お店側も保存しない——集計・提案・混雑はすべて評価の台帳から導出。店は最初の評価と同時に生まれる

## 用語 — 全体の用語集に追加する呼称

| 呼称（これで統一する） | 何を指すか | 言い換えない |
|---|---|---|
| 会員 | アプリにログインする部署の人。立案と参加の主体 | ユーザー、アカウント、利用者 |
| イベント | 1回の誘い。成立するか、流れるか、取り消されて終わる | 企画、飲み会、募集（Slack 投稿の表現としてだけ使う） |
| 参加 | イベントに「参加」を押した記録。取り消されていないものを**有効な参加**と呼ぶ | エントリー、出席、行ける（「3人いれば行ける」など口語の可否表現は例外。参加の言い換えとしてだけ禁止）、申し込み |
| コメント | 参加に添える短い文（例：「遅れて行きます」）。任意。開示まで誰にも見せない | メッセージ（Slack の「メッセージ ID」という技術語は例外）、メモ、備考 |
| 内容 | イベントで何をするか（例：「ランチ」「スマブラ大会」）。自由記述 | タイトル、題名、件名 |
| 場所 | 集合・開催の場所。**店から選ぶ・自由記述・空欄のどれでもよい**（会議室・集合地点も入る広い概念） | 会場、ロケーション（「店」はお店の文脈の正式呼称になったため禁止語から外した） |
| 募集中 | イベントの状態の1つ（`IS_RECRUITING`）。成立する前の状態 | 受付中、オープン |
| 参加者 | 有効な参加を持つ人。立案者を含む | メンバー、顔ぶれ |
| 立案者 | イベントを作った人。参加者の1人に数える。誰かは永久に表示しない | 発案者、主催者、幹事、作成者 |
| 成立人数 | 立案者を含む、成立に必要な人数 | 定員（上限ではなく下限）、最低人数 |
| 開始時刻 | イベントが始まる日時。**開催日を含む**（当日でも未来日でもよい）。参加の締切・流れの判定の基準 | 開催日時、集合時間、開催時刻 |
| 開示タイミング | 開始時刻のどれだけ前に開示するか。**既定は30分前**で、立案者がイベントごとに変えられる | 公開タイミング、表示時刻 |
| 成立 | 参加者の数が成立人数に達すること | 開催決定、マッチング |
| 確定型 | 成立人数1のイベント（作った瞬間に成立。「来たい人はどうぞ」の使い方） | 開催確定イベント |
| 開示 | **成立しているイベント**で、開示時刻（開始時刻 − 開示タイミング）から、参加者の一覧（名前・コメント）が URL を開いた人に見えるようになること。一覧が出るのは有効な参加が**2人以上**のとき（→ 判断6） | 公開、発表（「公開用 UUID」「インターネットに公開」など、開示と無関係の複合語は例外） |
| 流れる | 開始時刻までに成立しない、または開示前の人数割れで終わること | 不成立、失敗（Slack 投稿の失敗など技術的な意味では使う。流れるの言い換えとしてだけ禁止）、廃案 |
| （イベントの）取り消し | 開示前に立案者が参加を取り消して、イベントが終わること | 中止、取り下げ、キャンセル、削除（評価の物理削除という操作の語としては使う。イベントの取り消しの言い換えとしてだけ禁止） |
| 告知先 | ボットが投稿する Slack チャンネル | 通知先、連携チャンネル |
| 店 | 評価の対象。最初の評価と同時にマスタに載る | 飲食店、レストラン、ストア（「お店」は文脈の呼び名としてだけ使う） |
| 評価 | 店への1回の投稿。3段階＋任意の時間帯・並んだか・一言 | レビュー（画面の導線名「この店をレビューする」と、設計ドキュメントの査読の意味は例外。評価の言い換えとしては使わない）、口コミ、点数、評点 |
| 3段階 | 評価の値。**また行きたい／ふつう／いまいち** | 星、スコア |
| 時間帯 | 行った時間の区分。〜11時台／12時台／13時台／14時以降 | タイムスロット（「時刻」は開始時刻・開示時刻の構成語なので語自体は禁止せず、時間帯の言い換えとしてだけ使わない） |
| 一言 | 評価に添える記名の短い感想。**コメント（参加に添える・開示まで秘匿）とは別概念**（常時表示） | メッセージ、感想 |
| 店マスタ | マスタに載った店の全体（＝店エンティティの集合の呼び名）。独立したモデルではなく、取り込みバッチも無い | 店舗一覧、店データ |
| 混雑の声 | 「その時間帯に行った N件のうち、並んだのは M件」という表示（**件数**。人数ではない）。断定しない | 空き情報、混雑度 |
| 提案 | 「また行きたいが多い店」（**部署全体の評価**から導出）。イベント作成の場所選び・**開示後の詳細（店で確定していないイベント）**・ルートに出す。ルートでは「自分がまだ評価していない店」も1枠。**参加者の評価履歴・好き嫌いは使わない** | おすすめ、レコメンド |

## ER 図

お誘いとお店は一連の流れなので、1枚の図で全体の繋がりを示す。

![Osasoi全体のER図。3つのコンテキストを1枚に描く。左のudbに今回作る会員（User）があり、会員ID・公開用UUID・ログインID（email）・表示名（開示で見える名前）・状態を持つ。中央のeventに今回作るイベント（Event）と参加（EventEntry）がある。イベントはイベントID・公開用UUID・内容・場所（自由記述または店ID・任意）・立案者ID（永久に表示しない）・成立人数（1なら確定型）・開始時刻（開催日を含む）・開示タイミング・状態（募集中・成立・流れた・取り消された）を持つ。参加は参加ID・イベントID・会員ID・コメント（任意。開示まで見せない）・取り消し日時（Noneなら有効な参加。押し直しは新しい行）を持つ。右のshopに今回作る店（Shop）と評価（ShopReview）がある。店は店ID・相手側ID（地図API側のID。手で足した店は無し）・店名・緯度経度（任意）・場所メモ（任意。手で足した店の目印）を持つ。評価は評価ID・店ID・会員ID・3段階（また行きたい/ふつう/いまいち）・時間帯（任意）・並んだか（任意）・一言（任意・記名で常時表示）を持つ。多重度は6本——会員とイベントは1対多（立案者として）、会員と参加は1対多、イベントと参加は1対多、イベントと店は多対1（場所として選んだ店・任意）、店と評価は1対多、会員と評価は1対多。データを持たないものとして、参加者数（取り消し日時が空の参加を数えて求める。立案者の行も含む）、開示済み（成立していて、かつ現在時刻が開始時刻−開示タイミングを過ぎたか）、確定型（成立人数が1）、告知先（環境変数で持ち、モデルにしない）、また行きたいの人数（人数で数える。同じ人の複数評価は最新の1票）、混雑の声（6ヶ月以内・2件以上のときだけ）、時間帯フィルタの結果、提案の2枠（また行きたいが多い店／自分がまだ評価していない店。開くたびに導出）、店マスタの取り込みバッチは無い（店は最初の評価と同時に生まれる）ことを注記している。参照はevent→udb・shop→udb・event→shop（場所として選んだ店ID）の一方向で、shopはeventを一切知らない。5モデルとも今回作る](./img/er-osasoi.svg)

## EntityModel

5つとも今回このアプリのために設計する（会員・認証も設計対象 → 置き場所）。
`User` にイベント側の情報（参加履歴など）は1列も足さない。名前は `naming.md` で確定済み。

### <会員（User）>

ログインする人。立案者・参加者の主体。開示のときに参加者へ見えるのは `name`（表示名）。

```scala
case class User(
  id:        Option[Id],                // 管理 ID（永続化前は None）
  uuid:      UUID,                      // 公開用 UUID
  email:     String,                    // ログイン ID（一意）
  name:      String,                    // 表示名。開示のときに参加者へ見える名前
  state:     Status = Status.IS_ACTIVE, // 会員の状態（区分値）
  updatedAt: LocalDateTime = Now,       // データ更新日
  createdAt: LocalDateTime = Now        // データ作成日
) extends EntityModel[Id]

object User:

  type   Id   = Id.Repr
  type   UUID = UUID.Repr
  object Id extends Entity.Id[Long]

  /** 公開用 UUID */
  object UUID extends Entity.Id[String]:
    def generate: UUID = UUID(java.util.UUID.randomUUID.toString)

  /** 会員の状態 */
  enum Status(val code: Short) extends EnumStatus[Short]:
    case IS_INACTIVE extends Status(code = -100) // 無効（運用上の停止。退会機能は作らない → 付録A）
    case IS_ACTIVE   extends Status(code =  100) // 有効
```

### <イベント（Event）>

```scala
case class Event(
  id:                 Option[Id],                        // 管理 ID（永続化前は None）
  uuid:               UUID,                              // 公開用 UUID。URL にはこれを使う（列挙対策）
  plannerId:          User.Id,                           // 立案者（User と *:1）。画面には永久に出さない（→ 判断3）
  title:              String,                            // 内容（例："ランチ"、"スマブラ大会"）。自由記述・50字まで
  place:              Option[String] = None,             // 場所（自由記述・任意・100字。店を選ばないときに使う）
  shopId:             Option[Shop.Id] = None,            // 場所として選んだ店（Shop と *:1）。place が None のときだけ Some（→ 判断8）
  requiredEntryCount: Int,                               // 成立人数。立案者を含む頭数（1〜30）。1 なら確定型
  startAt:            LocalDateTime,                     // 開始時刻（開催日を含む。未来日も可）。締切・流れの基準
  revealBefore:       Duration = Duration.ofMinutes(30), // 開示タイミング。開始時刻のどれだけ前に開示するか（既定値の調整は HANDOFF.md）
  state:              Status = Status.IS_RECRUITING,     // イベントの状態（区分値、→ 判断1）
  updatedAt:          LocalDateTime = Now,               // データ更新日
  createdAt:          LocalDateTime = Now                // データ作成日
) extends EntityModel[Id]

object Event:

  type   Id   = Id.Repr
  type   UUID = UUID.Repr
  object Id extends Entity.Id[Long]

  /** 公開用 UUID */
  object UUID extends Entity.Id[String]:
    def generate: UUID = UUID(java.util.UUID.randomUUID.toString)

  /** イベントの状態 */
  enum Status(val code: Short) extends EnumStatus[Short]:
    case IS_CANCELED   extends Status(code = -200) // 取り消された（開示前に立案者が参加を取り消した）
    case IS_FAILED     extends Status(code = -100) // 流れた（開始時刻までに成立しなかった／開示前の人数割れ）
    case IS_RECRUITING extends Status(code =  100) // 募集中
    case IS_CONFIRMED  extends Status(code =  200) // 成立
```

### <参加（EventEntry）>

```scala
case class EventEntry(
  id:         Option[Id],                   // 管理 ID（永続化前は None）
  eventId:    Event.Id,                     // どのイベントへの参加か（Event と *:1）
  userId:     User.Id,                      // 誰の参加か（User と *:1）
  comment:    Option[String] = None,        // 参加に添える一言（例："遅れて行きます"）。開示まで誰にも見せない
  canceledAt: Option[LocalDateTime] = None, // 取り消した日時。None なら有効な参加。一度入れたら戻さない（押し直しは新しい行）
  updatedAt:  LocalDateTime = Now,          // データ更新日
  createdAt:  LocalDateTime = Now           // データ作成日 ＝「参加」を押した日時
) extends EntityModel[Id]

object EventEntry:

  type   Id = Id.Repr
  object Id extends Entity.Id[Long]
```

### <店（Shop）>

評価の対象。**最初の評価が書かれたときに生まれる**（単独で作る画面は無い）。

```scala
case class Shop(
  id:         Option[Id],                // 管理 ID（永続化前は None）
  externalId: Option[String] = None,     // 地図API側の ID。手で足した店は None
  name:       String,                    // 店名（50字まで）
  latitude:   Option[BigDecimal] = None, // 緯度。手で足した店は None のことがある
  longitude:  Option[BigDecimal] = None, // 経度。同上
  address:    Option[String] = None,     // 大まかな場所（自由記述・100字。手で足した店の目印用）
  updatedAt:  LocalDateTime = Now,       // データ更新日
  createdAt:  LocalDateTime = Now        // データ作成日 ＝ 最初の評価が書かれた日時
) extends EntityModel[Id]

object Shop:

  type   Id = Id.Repr
  object Id extends Entity.Id[Long]
```

### <評価（ShopReview）>

「誰が・どの店を・どう言ったか」の1件。**本人はいつでも書き直せる・消せる**。

```scala
case class ShopReview(
  id:        Option[Id],               // 管理 ID（永続化前は None）
  shopId:    Shop.Id,                  // どの店への評価か（Shop と *:1）
  userId:    User.Id,                  // 誰の評価か（User と *:1）。名前は常時表示される
  rating:    Rating,                   // 3段階（これだけで送信できる）
  timeSlot:  Option[TimeSlot] = None,  // 行った時間帯。昔の店は覚えていないので任意
  waited:    Option[Boolean]  = None,  // 並んだか。未回答を許す
  remark:    Option[String]   = None,  // 一言（100字・改行なし）。任意
  updatedAt: LocalDateTime = Now,      // データ更新日
  createdAt: LocalDateTime = Now       // データ作成日 ＝ 投稿日時（混雑の集計の6ヶ月判定に使う）
) extends EntityModel[Id]

object ShopReview:

  type   Id = Id.Repr
  object Id extends Entity.Id[Long]

  /** 3段階 */
  enum Rating(val code: Short) extends EnumStatus[Short]:
    case IS_DISAPPOINTED extends Rating(code = -1) // いまいち
    case IS_NEUTRAL      extends Rating(code =  1) // ふつう
    case IS_WANT_AGAIN   extends Rating(code =  2) // また行きたい

  /** 時間帯 */
  enum TimeSlot(val code: Short) extends EnumStatus[Short]:
    case IS_UNTIL_11  extends TimeSlot(code = 1) // 〜11時台
    case IS_AROUND_12 extends TimeSlot(code = 2) // 12時台
    case IS_AROUND_13 extends TimeSlot(code = 3) // 13時台
    case IS_FROM_14   extends TimeSlot(code = 4) // 14時以降
```

**ここで型が語っていること**（理由は付録Bの番号を参照）

- `EventEntry` は状態を持たない台帳に近い。有効か取り消し済みかは `canceledAt` の有無で分かる（→ 判断2）
- 参加者数・開示済み・確定型は保存しない。すべて計算で求める（求まるものは保存しない）
- 立案者の参加も `EventEntry` の1行。立案者かどうかは `Event.plannerId` との比較で分かる
- 告知先はモデルにしない。環境変数で持つ
- `ShopReview` は状態（Status）を持たない。台帳ではなく**上書き・削除を許す可変の記録**（
  お誘いの「追記のみ」原則からの意図的な逸脱）
- お店側の集計・提案はどこにも保存しない。すべて `ShopReview` から導出
- `Shop` に公開用 UUID は持たない。店の画面はすべてログイン必須で、隠すものがない

**型では守れない決めごと**

- 有効な参加（`canceledAt` が `None`）は、同じ人 × 同じイベントで**最大1行**。
  `canceledAt` は `None` に戻さない。押し直しは新しい行
- `comment` は本人が**開示より前なら**いつでも書き直せる（参加はそのまま）。
  開示後と、取り消し済みの行は編集不可。可変列だが履歴の根拠には使わない
- イベント作成時は `Event` と立案者の `EventEntry` を**同じトランザクションで**作る。
  成立人数1なら `IS_CONFIRMED` で作る（確定型）
- `state` の更新は「作る」「押す」「取り消す」「時刻で締める」の4つの操作関数に絞る。
  **成立しているかの正は `state`。** 参加者数から数え直さない（→ 判断1）
- 「参加」を押せるのは `IS_RECRUITING` / `IS_CONFIRMED` で、開始時刻より前のイベントだけ。
  **参加の取り消しも開始時刻より前まで**（開始時刻ちょうどはどちらも不可）
- 参加の追加・取り消しと状態遷移は、**イベント行をロックして同じトランザクションで**行う
  （二重押し・成立の二重投稿・立案者の取り消しとの競合を防ぐ → 判断5）
- 入力の上限（確定）: 内容50字・場所100字・コメント100字（改行なし）・成立人数1〜30・
  開始時刻は90日先まで・開示タイミングは0〜24時間（分単位の整数で保存する）。
  メールアドレスの一意判定は大文字小文字を同一視する
- `place` と `shopId` は**同時に入れない**（どちらか一方、または両方 `None`）。アプリ側で検証する
  （MySQL の CHECK 制約は雛形に先行例が無い）。API 直叩き・「同じ内容で新しく作る」の写しにも
  適用し、テストで守る（→ HANDOFF.md）
- `shopId` を入れるときは、指す `Shop` が存在することを確認する。参照先が引けなくなったイベントは
  場所を「場所未設定」と表示し、レビュー導線は店の検索に切り替える（評価0件の店を画面に
  出さないのは店リストと提案だけで、イベントから参照されている店名は表示してよい）
- `requiredEntryCount`（1以上）・`plannerId`・`revealBefore`・`place`・`shopId` は作成後に変更しない。
  **唯一の例外は、空欄だった場所の後追い確定**（開示後に `shopId` を1回だけ入れる。
  イベント行をロックして行い、二重確定を防ぐ → 判断9）
- `startAt` は開催日を含む日時。当日でも未来日でもよいが、**立案者が1つに決める**（候補日は持たない → 付録A「日程調整」）
- `revealBefore` は0以上。**開示時刻（`startAt` − `revealBefore`）は作成時点より未来で
  なければならない**（ちょうども不可。確定型が作った瞬間に開示される抜け道を塞ぐ）
- 時刻はすべて日本時間（JST）固定で扱う（サーバー・DB とも。**アプリ全体の決めごとで、
  お店の6ヶ月判定・時間帯にも適用する**）。スケジューラは1分間隔で、
  「流れた」の投稿が数分遅れることは許容する

**型では守れない決めごと（お店）**

- `Shop` は最初の評価と**同じトランザクションで**作る。評価0件の `Shop` を作る経路は無い。
  最後の評価が消えても `Shop` は残す（評価0件の店は画面に出さないだけ）
- 同じ `externalId` の店は2行作れない（DB の一意制約で防ぐ。`None` どうしの重複は可）
- `Rating` の code はマイナス＝否定的、プラス＝肯定的（状態ではないので100刻みは適用しない）
- 「また行きたい」の集計は**人数**（同じ人の複数評価は1人と数え、`createdAt` が最新の `rating` を使う）。
  混雑の集計は**件数**で、母数は「`createdAt` が6ヶ月以内で、その時間帯の `waited` に回答がある評価」。
  母数が2件以上のときだけ表示する
- 入力の上限: 店名50字・場所メモ100字・一言100字（改行なし）。字数は文字（コードポイント）で
  数え、前後の空白は除いて保存する（お誘い側の入力にも共通のポリシー）
- `externalId` は `<プロバイダ>:<相手側 ID>` の形で保存する（例: `osm:9012`）。
  地図APIを乗り換えても接頭辞で区別できる
- 店の詳細の評価一覧に**投稿日時を表示しない**（`createdAt` は6ヶ月判定と「最新の `rating`」判定の
  内部利用のみ）。並びは**表示名順（文字コード順）**——投稿順や日時を見せると、開催直後に現れた
  記名の評価とイベントの開始時刻を突き合わせて、参加者・立案者が逆算できるため
  （判断6 が塞いだ穴の shop 側 → 判断8 の代償）
- 各店に「**Google マップで見る**」リンクを置く。座標があれば座標、無ければ店名の検索 URL。
  **外部 API は呼ばない・外部の評価は取り込まない**（→ 付録A。URL の形式は HANDOFF.md）
- お店にスケジューラ・バッチは無い

**保存されるデータの例**（会員 A〜D の部署）

`Event`

| id | plannerId | title | requiredEntryCount | startAt | state | 何が起きたか |
|---|---|---|---|---|---|---|
| 21 | A | 今日ランチ | 3 | 12:00 | IS_CONFIRMED | A が作成 → B・C が押して3人で成立 |
| 22 | B | スマブラ大会 | 1 | 19:00 | IS_CONFIRMED | 確定型。**作った瞬間から成立** |
| 23 | C | 飲み | 4 | 19:30 | IS_FAILED | 2人までしか集まらず、開始時刻が来て流れた |
| 24 | D | 朝ランニング | 2 | 8/23 07:00 | IS_RECRUITING | **5日後の未来日イベント**。日付を指定して募集中 |

`EventEntry`（イベント21の分）

| id | eventId | userId | canceledAt | createdAt | 何が起きたか |
|---|---|---|---|---|---|
| 101 | 21 | A | `None` | 10:02 | 立案者の参加。イベント作成と同時にできる |
| 102 | 21 | B | **10:31** | 10:15 | B は一度押して取り消した |
| 103 | 21 | C | `None` | 10:40 | C が押して2人 |
| 104 | 21 | B | `None` | 11:05 | **B の押し直し（行102は書き換えず、新しい行）**。3人で成立 |

B が迷った履歴（行102・104）はそのまま残るが、画面に出るのは有効な参加だけ。

**保存されるデータの例（お店）**（同じ会員 A・B）

`Shop`

| id | externalId | name | latitude/longitude | address | どうやって載ったか |
|---|---|---|---|---|---|
| 31 | osm:9012 | 大三元 | あり | `None` | A が検索して選び、最初の評価を書いた |
| 32 | `None` | まるまる食堂 | `None` | 「郵便局の裏」 | 検索で見つからず、B が手で足した |

`ShopReview`

| id | shopId | userId | rating | timeSlot | waited | remark |
|---|---|---|---|---|---|---|
| 201 | 31 | A | IS_WANT_AGAIN | IS_AROUND_12 | true | 回鍋肉がやばい。12時台は並ぶ |
| 202 | 31 | B | IS_NEUTRAL | `None` | `None` | `None` |
| 203 | 31 | A | IS_WANT_AGAIN | IS_FROM_14 | false | 14時なら並ばない |
| 204 | 32 | B | IS_WANT_AGAIN | IS_AROUND_13 | false | `None` |

- 大三元の「また行きたい」は**2人**（A は2件書いているが1人と数える）
- 大三元の14時以降の混雑の声は1件なので**まだ表示しない**（2件未満）
- B の評価202は3段階だけの最小投稿（時間帯・並び・一言すべて未回答）——これが許される形

## 区分値の考え方

マイナスが終了、プラスが進行中〜正常。遷移は4本だけ。

![Event.stateの遷移図。募集中IS_RECRUITING（100）と成立IS_CONFIRMED（200）が進行中の状態（プラス）、流れたIS_FAILED（-100）と取り消されたIS_CANCELED（-200）が終了の状態（マイナス）。遷移は4本: ①募集中→成立（参加者数が成立人数に達した。成立を投稿）②募集中→流れた（開始時刻が来ても成立しなかった。流れたを投稿）③成立→流れた（開示前の取り消しで人数割れ。流れたを投稿）④募集中または成立→取り消された（開示前に立案者が参加を取り消した。取り消されたを投稿）。注記: 確定型（成立人数1）は作成の瞬間から成立で始まる。成立のまま開始時刻を過ぎたら開催とみなし状態は変えない。開示より後は人数が割れても0人になっても成立のまま。流れた・取り消されたには時刻が来ても何もしない](./img/event-state.svg)

- 「成立」を参加者数からの導出にしないのは、**開示後は人数が割れても成立のままにする**
  （数と状態がわざと食い違う）仕様のため（→ 判断1）
- **開示済みは状態に入れない。** 「成立している かつ 現在時刻 ≥ 開始時刻 − 開示タイミング」で求まる（→ 判断4）
- **開催済みも状態に入れない。** `IS_CONFIRMED` のまま開始時刻を過ぎたら開催とみなす

## 判定ルール

### いつ何をするか

| きっかけ | すること |
|---|---|
| イベントを作った | `Event` と立案者の `EventEntry` を1行ずつ作る（成立人数1なら `IS_CONFIRMED` で）。ボットが募集を投稿（名前は出さない。**確定型は募集の投稿が成立を兼ね、別途の成立投稿はしない**。体裁は「やります。来たい人どうぞ」） |
| 「参加」を押した | 有効な参加が無いことを確認して1行足す。**`IS_RECRUITING` で**成立人数に達したら `IS_CONFIRMED` にして成立を投稿（**遷移が起きた1回だけ。成立後の追加参加では投稿しない**） |
| 参加を取り消した | `canceledAt` を書く。**開示前**に成立人数を割ったら `IS_FAILED` にして流れたを投稿。**開示後**は `state` を変えない（一覧から消えるだけ。0人になっても状態は変えず、表示だけ「参加者がいなくなりました」にする） |
| 立案者が参加を取り消した | **開示前**：`IS_CANCELED` にして取り消されたを投稿（人数は関係ない。確定型を止める唯一の手段）。押す前に**本人の画面にだけ**「イベントごと取り消される」ことの確認を出す（本人にしか見えないので匿名は破れない）。**開示後**：普通の取り消しと同じ（→ 判断3） |
| 開始時刻が来た | `IS_RECRUITING` のままなら `IS_FAILED` にして流れたを投稿。`IS_CONFIRMED` はそのまま（開催とみなす）。`IS_FAILED` / `IS_CANCELED` には何もしない。**この判定は分単位のスケジューラが実行する**（画面を開いたときの判定だと、誰も開かなければ投稿されないため）。表示の切り替え（提案枠・レビュー導線）はスケジューラの仕事ではない（→ 次の2行） |
| 開示時刻（開始時刻 − 開示タイミング）が来た | **何も書き込まない**。成立していれば詳細ページの表示が変わるだけ（スケジューラも不要）。未成立なら何も起きない。ボットの追加投稿もしない。**店で確定していないイベント（`shopId` が空）には、あわせて提案（部署全体の評価から・1枠）と「部署のお店を見る →」の導線を出す**。場所が空欄のままなら、有効な参加を持つ人は「**この店に決めた**」で場所を1回だけ確定できる（→ 判断9） |
| 開始時刻を過ぎた（レビュー導線） | **何も書き込まない**（開催の事実は記録しない → 付録A）。`IS_CONFIRMED` かつ 現在時刻 ≥ 開始時刻 かつ**ログイン済み**のとき、詳細に「この店をレビューする」導線を出す（`shopId` ありならその店の**新規の評価フォーム**へ、無ければ店の検索へ）。提案枠と店の導線はここで下げ、レビュー導線に入れ替える。表示の期限は設けない |

ボットの投稿は**募集1本＋状態変化ごとに1本**（通常2本。**確定型は募集が成立を兼ねるので
通常1本**。成立の後に流れた・取り消された場合だけ3本）。どの投稿にも内容・開始時刻・
成立人数・詳細ページの URL を入れ、成立の投稿には開示時刻も明記する
（どのイベントの話か、投稿単体で分かるように）。
投稿は状態の変更と**切り離す**。正はあくまで状態で、投稿できなくても
イベントの状態は巻き戻さない。再試行や投稿の編集もやらない（→ 付録A）。

### 誰に何が見えるか

**開示は「成立している」＋「開示時刻を過ぎている」の両方がそろって起きる**（→ 判断4）。
未成立のまま開示時刻を過ぎても、その後に成立するまでは何も見せない。

![開示のタイムライン図。時間軸上に作成（募集を投稿）→成立（人数が達した時に投稿。確定型は作成と同時）→開示時刻（開始時刻−開示タイミング。既定30分前・変更可）→開始時刻（参加・取り消しの締切。成立のままなら開催とみなす）が並ぶ。作成から開示時刻までは「匿名の世界」: 誰が押したか・コメントは誰にも見えない（成立するまでは人数も見えず、成立後は人数だけ見える）。開示時刻から開始時刻までは「記名の世界」: 参加者の一覧（名前・コメント）がURLを開いた人なら誰でも見え、取り消しも一覧に反映されて見える。注記: 開示は成立が前提（未成立なら開示時刻を過ぎても何も見えない）。開示前に終わったイベントは誰が押していたか永久に誰にも見えない](./img/reveal-timeline.svg)

| 情報 | 成立前 | 成立後〜開示前 | 開示後 |
|---|---|---|---|
| 内容・開始時刻・場所・開示時刻 | URL を開けば誰でも | 同左 | 同左 |
| 成立人数（何人で成立か）・イベントの状態 | 誰でも | 誰でも | 誰でも |
| 集まっている**人数** | **誰にも見えない** | **誰でも**見える | 誰でも見える |
| 参加者の**一覧**（名前・コメント） | 誰にも見えない | 誰にも見えない | **URL を開いた人なら誰でも**見える（有効な参加のみ。**2人以上のときだけ**。並びは**表示名順（文字コード順）** → 判断6） |
| 立案者が誰か | 誰にも見えない | 誰にも見えない | 誰にも見えない（**永久**） |
| 自分が押しているか・自分のコメント | 本人にだけ見える | 本人にだけ見える | 本人にだけ見える（一覧にも載る） |

流れた・取り消されたイベントは、成立しないまま（または開示前に）終わるため**開示は一度も
起きない**。誰が押していたかは永久に誰にも見えず、集まっていた人数も出さない（結末の表示だけ）。
イベントの取り消しでは、他の参加者の `canceledAt` は書かない（取り消したのは立案者だけ。
イベントの状態が終了を表す）。

**イベントの一覧（ルートの上段）はログイン必須**。載せるのは `IS_RECRUITING` / `IS_CONFIRMED` で
`startAt` が7日先までのイベントと、**開始時刻を過ぎた `IS_CONFIRMED` のイベント（当日いっぱい）**——
後者は「食べたら、そのままレビューを書ける」に戻る経路。集まっている人数は一覧には載せない。
過去の振り返り画面は作らない（→ 付録A）。

Slack に出してよいのは、この表の上2行（内容・開始時刻・場所・開示時刻・成立人数・状態）の
範囲に限る。`shopId` のイベントは、投稿・プレビューの「場所」に**店名だけ**を載せる
（座標・評価・リンクは載せない）。**集まっている人数・立案者・参加者の名前は Slack には絶対に載せない**
（転送できて永久に残るため。アプリ内なら取り消しが即反映される）。

参加者数は、その `eventId` の `EventEntry` のうち `canceledAt` が `None` の行数（立案者の行も含む）。
この数を使ってよいのは、遷移の判定・成立後の人数表示・開示後の一覧表示で、
「成立しているか」は `state` を見る。

### お店の導出ルール

| 出すもの | 求め方 | 出さない条件 |
|---|---|---|
| また行きたい N人 | その店の評価者ごとに最新の `rating` を取り、`IS_WANT_AGAIN` の人数 | 0人なら表示しない |
| 混雑の声 | 時間帯ごとに「その時間帯に行った N件のうち、並んだのは M件」（母数は `waited` 回答ありの評価） | その時間帯の6ヶ月以内・`waited` 回答ありが2件未満なら「まだ声がありません」 |
| 提案①（また行きたいが多い店） | 全店から人数最多を1件（同数のときは、その店の**最新の評価の `createdAt`** が新しい方）。**出す場面: イベント作成で「店から選ぶ」を開いたとき（店検索の初期表示）・開示後の詳細（店で確定していないイベント）・ルート** | 店で確定済み（`shopId` あり）のイベントには出さない。全店0人のときは枠を消さず**空状態**を出す（「まだ部署の評価がありません。行ったお店を書くと、次のお誘いから提案が出ます」＋評価を書く導線） |
| 提案②（まだ評価していない店） | 自分の評価が無い店から、また行きたいの人数最多を1件。**ルートにだけ**出す（本人向け） | 該当なしなら枠ごと出さない |
| 時間帯フィルタ | その時間帯の評価が付いた店だけに絞り、その時間帯の声だけを見せる | — |

すべて開くたびに導出し、保存しない。評価の書き直し・削除には自動で追従する
（過去に見た数字と食い違いうることは質問リストで許容済み）。

## 置き場所（コンテキスト）

**3つのコンテキスト（`udb` / `event` / `shop`）は、すべて今回このアプリのために新設する**（→ naming.md）。

- **会員（`User`）は `udb`（会員・認証）に置く。** 雛形に同名の `udb` があるが別用途のため使わず、
  実装時に雛形の `User.scala` をこの設計で置き換える。認証の文脈として参照される側に保ち、
  イベントの都合の情報は持たせない
- **イベント・参加は `event` に置く。** どちらも `User.Id` を参照する側なので `udb` には置けない。
  参照は `event → udb` の一方向
- **店・評価は `shop` に置く。** `shop → udb` の一方向。
  **`event → shop` は「場所として選んだ店の ID」の一方向参照だけ**（→ 判断8）。
  shop は event を一切知らない（評価・提案の導出にイベントの情報は使わない）
- テーブルは `udb_user` / `event` / `event_entry` / `shop` / `shop_review`
  （エンティティ名がコンテキスト名で始まるときは重ねない規則）

---

# 付録A：今回やらないこと

テーマごとにまとめる。理由が判断の記録にあるものは番号だけ示す。
ここに無い機能は「要求に無いから作らない」であり、個別には挙げない。

**お誘い**

- **イベントを膨らませる機能**——日程調整（成立条件を人数の一軸に保つ）、イベントの編集（押した人の前提を裏切る変更になるため。例外は場所の
  後追い確定 → 判断9）、コメント欄・チャット（参加に添える一言コメントは作る）、
  「行けたら行く」などの曖昧な回答、開催の記録・実施報告・精算（成立のまま開始時刻を過ぎたら開催とみなす）
- **人を管理する機能**——参加の承認制・宛先の手選び・複数チャンネル（境界は URL が届く範囲に委ねる）、
  立案者の記名オプション、管理者によるイベントの強制取り消し・通報（100人級に広げるときに
  管理者アクターごと検討）
- **通知を増やす・整える機能**——リマインド投稿、個人向け通知（DM）、
  開示のお知らせ投稿（→ 判断4）、Slack 投稿の再試行・編集・削除とメッセージ ID の保存、
  投稿のランダム遅延、プレビューの保証（非公開運用ならプレビューは出ないだけ）
- **過去イベントの一覧・振り返り・集計画面**（データは残す。開催前の一覧と、開始後
  当日いっぱいの表示——レビュー導線へ戻る経路——は作る → HANDOFF.md）

**お店**

- **外部データ・地図**——周辺全店の取り込み・定期再取得（店は評価の初回投稿で載る）、外部の評価の取り込み・
  埋め込み（「Google マップで見る」リンクだけ置く）、地図UI（リストのみ）
- **ゲーム・ソーシャル要素**——訪問の証明・スタンプ・称号・制覇（要求で明示的に不要）、
  常連バッジ・フォロー（一言に名前が出るだけ）
- **管理・モデレーション**——店の重複統合・編集画面（別名の2行は許容。直すなら管理者が DB で対応）、評価への承認・通報・
  他人による削除（消せるのは本人だけ）、評価の変更ログ（上書き・削除を許すため）、店の集計・振り返り画面
- **参加者に合わせた提案**——「このメンバーだと」・苦手の考慮（→ 判断7。次の要求の候補）

**アプリ全体**

- **会員まわりの省略**——パスワードの再設定・確認メール（忘れたら管理者が DB で対応）、
  退会（`IS_INACTIVE` は運用停止用。停止会員の特別扱いもしない——参加・一覧・立案はそのまま進む）
- **構成の省略**——タブ・ボトムナビ等のナビゲーション部品（ルートの縦積みで足りる）、
  オフライン対応、複数サーバー構成（1台前提 → 判断5）、設定画面（告知先・検索の中心は環境変数）

# 付録B：判断の記録

## ① 当初の要求の範囲で決めたこと

全9件（追加の要求はまだ無い）。番号は通し番号。
**記録するのは設計の骨格を決めた判断だけ**——1行で言い切れる細かい取捨は、
付録Aと本文の決めごと・導出ルールに畳んである。

### **1. イベントの状態（`state`）を保存するか、導出するか**

**保存する（4値）。** 「流れた」「取り消された」は出来事で、参加の行からは求まらない。
「成立」も、開示後は人数が割れても成立のままにする仕様のため、**数と状態はわざと食い違う**。
却下案は全部導出——この仕様を表現できない。
**代償**：参加者数との二重管理。更新経路を4つの操作関数（作る・押す・取り消す・時刻で締める）に絞って対応する。

### **2. 参加の取り消しを、行の削除で表すか、取り消し日時の追記で表すか**

**`canceledAt` の追記。行は消さず、押し直しは新しい行。**
却下案は物理削除（取り消した事実ごと消える）と、`canceledAt` を `None` に戻す案
（可変な列を履歴の根拠にすることになる）。
残した履歴の読み手は**分析ではなく調査**（問い合わせ対応）。画面には一切出さない。
**代償**：有効な参加を探す条件に常に `canceledAt IS NULL` が付く。忘れると取り消した人を数えてしまう。

### **3. 立案者の取り消しの扱い（01 の質問リストから引き継ぎ）**

**開示前はイベントごと取り消し（`IS_CANCELED`）、開示後は普通の取り消しと同じ。**
専用の取り下げボタンは作らない（匿名の立案者にだけ出すボタンは、それ自体が正体の手がかりになる）。
開示後に限って通常扱いにするのは、一覧から消えた人と同時にイベントが消えると立案者だと推測できるため。
**代償**：開示後は誰もイベントを止められない。相談は開示された参加者同士に委ねる。

### **4. 開示の条件と、開示後に誰が見られるか**

開示まわりを見直した回の判断を、5つまとめて記録する。

- **開示は「成立している」が前提。** 時刻だけで開示すると、未成立のまま開示時刻を過ぎた
  イベントで押した人の名前が露出し、そのまま流れたら「集まらなくても誰も傷つかない」が破れる。
  開示時刻の後に成立したら、成立と同時に開示する（開示済みは導出なので自動で成り立つ）
- **開示後の一覧は URL を開いた人なら誰でも見られる**（当初は参加者限定だった）。
  開示後はもう記名の世界で隠す秘密が無く、見えることが駆け込み参加を誘う。
  ただし Slack には名前を載せない（転送できて永久に残る。アプリ内なら取り消しが即反映）
- **成立後は人数だけ誰でも見える**（名前は開示まで伏せる）。「結構集まってるな」という
  判断材料になる。募集中に人数を隠すルールは成立前だけの話なので矛盾しない
- ボットの投稿は**募集1本＋状態変化ごとに1本**（通常2本。確定型は募集が成立を兼ねるので
  通常1本。成立の後に流れた・取り消された場合だけ3本）。開示のお知らせ投稿はせず、
  詳細ページに開示時刻を常時表示する（却下案：開示時の追加投稿——チャンネルのノイズが増える）
- 開示の一覧には**最低表示人数（2人）と並び順（表示名順・文字コード順）**の条件がある（詳細は判断6）

**代償**：開示済みの判定が「`state` × 時刻」の2条件になる。
開示の瞬間を知らせる手段がアプリに無いので、開示時刻を自分で見て開きに来ることになる。
なお**開示の条件の正はこの判断4**。他の箇所の記述はここを参照する。

### **5. 同時操作をどう守るか**

**参加の追加・取り消しと状態遷移は、イベント行をロックして1トランザクションで行う（悲観ロック）。**
守るもの: ①同じ人の二重押しで有効な参加が2行できない ②2人が同時に成立ラインを踏んでも
成立の遷移と投稿は1回だけ ③「押す」と「立案者の取り消し」が同時でも直列化される。
却下案は DB の一意制約（「`canceledAt` が `None` の行は1つ」は条件付き一意で、
MySQL では単純なユニークインデックスで表現できない）と、楽観ロック（列が増え、
失敗時のリトライ設計が要る）。
**代償**：同じイベントへの操作が直列化される（30人規模では問題にならない）。
型でも制約でも守れないため、テストで守る。

### **6. 開示の一覧に最低表示人数を設けるか**

**設ける。有効な参加が2人以上のときだけ一覧（名前・コメント）を表示**し、
1人の間は「参加者1人（名前は2人から表示）」とする。
理由: 確定型（成立人数1）で誰も乗らないまま開示されると、一覧に並ぶのは立案者1人
＝**永久匿名が破れる**。成立人数2で開示後に1人が取り消した場合も同じ。
却下案は、確定型に限り記名と割り切る案（記名オプションはやらないと決めた方針と衝突）。
並び順は**表示名順（文字コード順）**にする——押した順だと、立案者の行は作成と同時にできるため
**一覧の先頭が必ず立案者**になり、順序から特定できてしまう。当初は「五十音順」としたが、
表示名に読み仮名が無く漢字は五十音に並ばないため、実装できる形に改めた
（目的は「押した順にしない」ことで、コード順で足りる。読み仮名の列は増やさない）。
**この閾値の限界も明記する**: 2人以上ルールが効くのは**第三者**だけで、参加者本人には
効かない。参加者がちょうど2人のとき、自分は立案者でないと知っている本人には、
もう1人が立案者だと確定する（3人でも2分の1に絞られる）。これは現実として防げない
（2人で行けば誘い主は分かる）。「立案者は永久に表示しない」は**画面に表示しない**という
約束であり、推測まで防ぐ約束ではない。画面にこの注意書きは出さない（ハードルを上げるため）。
**代償**：開示後の表示が人数で変わる分岐が1つ増える。2人イベントで開示後に1人が
取り消すと、一覧は人数表示に戻る（すでに見た人の記憶までは消せない）。
そのとき**残った本人には自分の参加とコメントを見せ続ける**（自分の状態を確認できない
不安の方が大きい）。

### **7. 提案をどの場面に・何から作るか**

**イベント作成の場所選び（「店から選ぶ」の検索初期表示）・開示後の詳細（店で確定していないとき）・ルートの3場面に出す。
中身は常に部署全体の評価**（また行きたいが多い店）で、**参加者の評価履歴・好き嫌いは使わない**。
この判断の核は後者——「参加者の誰も評価していない店」のような参加者ベースの提案は、
記名で常時表示される評価と突き合わせると**参加者の否定情報として逆算できる**
（判断6 が塞いだ穴を開け直す）ため、場面がどこであっても作らない。
経緯: 当初は「開示後では店を決めるには遅い」として本人向け・常時表示に限定したが、
発注者の実際の流れは「**開示＝みんなでどこ行こうの瞬間**」であり、そこに全体ベースの
提案を出すのはむしろ最適と確認して場面を広げた。
**代償**：要求文の「このメンバーだと」には応えない（→ 付録A「参加者に合わせた提案」）。
場所を店で確定済みのイベントには提案を出さない（もう決まっているため）。

### **8. イベントの場所を、店マスタへの参照に昇格する**

**`Event.shopId: Option[Shop.Id]` を足す。** 作成時に「店から選ぶ・自由記述（`place`）・空欄」の
どれでもよい（**選ばないことが多い**、が発注者の前提）。
これは当初見送って予約していた「`place` の参照昇格」の発動——お誘いとお店は
別案件ではなく、誘いを立てる→どこ行こう→開催→レビューの**一連の流れ**だという要求に応える。
参照は `event → shop` の一方向（ID のみ）。shop 側は event を一切知らない。
却下案は結合ゼロの維持（店名を手で書き写す）——一連の流れという要求に応えられず、
店を選んでもレビュー導線・店情報に繋がらない。
**代償**：`shop` のモデル変更が `event` に波及しうる（ID 参照のみに限定して抑える）。
場所の持ち方が2列（`place` / `shopId`）になる（**同時には入れない**ので、表示は入っている方を使う）。
**選んだ店と記名の評価を突き合わせると、立案者が推測されうる**（「その店を評価しているのは
1人だけ」が30人規模では普通に起き、手で足した店なら実質確定する）——判断6 と同じく
「画面に表示しない」約束であって推測を防ぐ約束ではないが、こちらは回避手段（空欄にする）が
本人にあるため、作成フォームに注意書きを1行出す（→ HANDOFF.md）。
また、管理者が DB で店の行を消すと `event.shop_id` が孤児になる（コンテキストをまたぐ FK は
張らない方針のため。「場所未設定」として表示するフォールバックで受ける）。

### **9. 開示後に、空欄だった場所を店で確定できるようにする**

**許す（1回だけ）。** 条件: 開示後・**有効な参加を持つ人なら誰でも**・`place` と `shopId` が
ともに空のときだけ・上書き不可。確定したら提案枠と店の導線は閉じる（画面は開示後の提案枠の
「**この店に決めた**」ボタン → HANDOFF.md）。
場所が空欄のまま開示で店を決めても、結果がアプリに残らないと、開始後のレビュー導線が
毎回店の検索に落ちる——誘い→どこ行こう→開催→レビューという一連の流れの、最後の継ぎ目が
外れるため。イベントを編集不可とした原則（→ 付録A「イベントの編集」）の理由は「すでに押した人の前提を裏切る変更」であり、
**未定を承知で押した人の前提を、未定が埋まることは裏切らない**（付録Aに例外として明記）。
立案者に限定しないのは、限定すると「立案者にだけ出るボタン」＝正体の手がかりになるため
（判断3 と同じ理由）。
却下案: 書き戻さない（当初案）——継ぎ目が外れたまま。立案者だけが確定できる案——匿名が破れる。
何回でも変えられる案——押した後に場所が転々とし、編集禁止の理由がそのまま当たる。
**代償**：イベント不変の原則に例外が1つできる。「誰が確定したか」は保存しない
（保存すると記名の情報が増え、確定した人が推測の材料になる）ため、間違った店で確定されても
直せない——正しい店はレビュー時に検索で選び直す。同時確定はイベント行のロック（判断5）で直列化する。

