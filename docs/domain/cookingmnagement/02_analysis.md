# <調理管理> — 思考過程（メモ・提出しない）

## 名詞と動詞
**名詞**：店舗、タブレット、注文、注文一覧、注文の状態、受け取り時間、商品、材料、
売り切れ、提供可、混雑、閾値、閉店、店舗スタッフ、会員、受付番号

**動詞**：見る、(調理中を)押す、調理する、(受取準備完了に)する、受け渡す、
売り切れにする、(提供可に)戻す、並べる、判定する、未受け取りにする、数える


| 種類 | 動詞 | 行き先 |
|---|---|---|
|状態を変える|調理する / 受取準備完了にする / 受け渡す / 未受け取りにする|Order.Status の遷移。うち未受け取りだけが新しい値|
|状態を変える|売り切れにする / 提供可に戻す|ShopMenuStock の区分値の遷移（既存。新しい値は増えない）|
|値を返す|混雑しているか判定する / 調理中の注文数を数える / 一覧を並べる|モデルのメソッド|


## ふるい分け
| 名詞 | 仕分け（エンティティ / 値オブジェクト / 属性 / 対象外 / 既存） | 理由 |
|---|---|---|
| 店舗 | 既存エンティティ（`Shop`） | タブレットはこの店舗のものとして動く。今回は変更しない |
| 会員 | 既存エンティティ（`User`） | 注文の持ち主。今回は変更しない |
| 注文 | 既存エンティティ（`Order`） | この演習の中心。区分値を1つ追加するが、属性は増やさない |
| 商品 | 既存エンティティ（`MenuItem`） | 売り切れにする対象。商品そのものは変更しない |
| 売り切れ / 提供可 | 既存エンティティ（`ShopMenuStock`）の区分値 | 店舗 × 商品の取扱状況として、すでに存在する |
| 注文の状態 | 既存の属性（`Order.Status`） | 「未受け取り」を値として1つ追加する |
| 未受け取り | 区分値の追加 | 新しいエンティティではない。`Order.Status` の6番目の値 |
| 受け取り時間 | 既存の属性（`Order`） | 並び替えに使うだけ。持ち方は変わらない |
| 受付番号 | 既存の属性（`Order.Code`） | 店頭での照合に使う。変更しない |
| 注文一覧 | 対象外（＝取得結果） | 保存しない。その店舗の注文を条件で絞って並べたもの |
| 混雑 | 対象外（＝計算結果） | 保存しない。調理中の注文数が閾値以上かで判定する |
| 閾値 | 対象外（設定値） | 業務ルールの定数。モデルの属性ではない |
| 材料 | 対象外 | 在庫管理はスコープ外。切れたという事実を売り切れとして表すだけ |
| 閉店 | 対象外 | 既存の営業時間で表せる。スタッフの操作タイミングでしかない |
| 店舗スタッフ | 対象外 | 個人を識別しない。誰が操作したかを記録しないと決めた |
| タブレット | 対象外（業務の外） | 操作する端末であって、業務の概念ではない |

## ユビキタス言語（候補）
| 日本語 | 英語名 | 意味 | 多重度・備考 |
|---|---|---|---|
| 未受け取り | `Order.Status.IS_NOT_PICKED_UP` | 受取準備完了のまま閉店を迎え、会員が受け取りに来なかった注文 | `Order.Status` に追加する6番目の値 |
| 混雑 | （モデルなし） | その店舗で調理中の注文が閾値以上ある状態 | 保存しない。件数を数えて判定する |
| 注文一覧 | （モデルなし） | タブレットに表示する、その店舗の未完了の注文 | 保存しない。条件で絞って並べた取得結果 |

## 名前を並べて確認する

今回追加するエンティティは無いためクラス名、テーブル名を並べない
新しく名前を付けるのは、`Order.Status` に追加する区分値 1 つだけ。既存の 5 つと並べて確認する。

```
IS_RECEIVED       受付
IS_COOKING        調理中
IS_READY          受取準備完了
IS_DELIVERED      受渡し完了
IS_CANCELED       キャンセル
IS_NOT_PICKED_UP  未受け取り（今回追加）
```

既存 5 つの英語名は仮。実際の `Order.scala` を確認して揃える。

見るポイントは 2 つ。
- **終端の対として読めるか。** `IS_DELIVERED`（渡せた）と `IS_NOT_PICKED_UP`（渡せなかった）が並ぶ。
どちらも注文の終わりで、結果が逆になっている

- **`IS_CANCELED` と混同しないか。**
キャンセルは作る前に取り消されたもの、未受け取りは作ったが渡せなかったもの。
**作ったかどうか**が違う。
この区別は、要件定義の「受付・調理中のまま閉店を迎えた注文はキャンセルとして扱う」と対応している


## コンテキストの判断
| 日常的に書き換える人 | 何を書き換えるか | 更新の頻度 | 見る画面 |
|---|---|---|---|
| 店舗スタッフ | 注文の状態（調理中 / 受取準備完了 / 受渡し完了 / 未受け取り） | 毎分 | 店舗のタブレット |
| 店舗スタッフ | 商品の売り切れ / 提供可 | 日常的 | 店舗のタブレット |

**判断：** 新しいコンテキストは作らない。何も動かさない。

今回のスコープでは新しいエンティティを作らないため、そもそも「どこに置くか」を決める必要がない。触る対象は 2 つあり、それぞれ既存の場所にある。

| 触る対象 | 所属コンテキスト | 今回すること |
|---|---|---|
| `Order.Status` | `sales` | 区分値を未受け取りを追加する |
| `ShopMenuStock` | `shop` | 既存の区分値を切り替えるだけ。変更なし |

**理由：** `Order.Status` の遷移（調理中・受取準備完了・受渡し完了）は、
02_BURGER_DOMAIN の時点ですでに `sales` にあり、店舗スタッフが触る前提で設計されている。
今回はそこに未受け取りを足すだけで、置き場所は変わらない。
売り切れも `ShopMenuStock` として `shop` にあり、店舗の人員が触るものとして位置づけられている。

画面とコンテキストは1対1ではない。店舗のタブレットはsalesのOrderとshopのShopMenuStockの両方を触る。
02_BURGER_DOMAINは「画面と権限が揃う」ことを利点に挙げているが、
これは分割の結果そうなりやすいという話であって、画面を基準に分けるという意味ではない。

基準はあくまで「誰が日常的に書き換えるか」で、
同じ人が別々の性質のデータを触るなら、1つの画面が複数のコンテキストにまたがる。
